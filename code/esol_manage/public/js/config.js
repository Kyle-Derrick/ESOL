let baseApiUrl = "http://127.0.0.1:8088";
let basePath = "/manage";
/**
 * 前端的一些配置,比如跨域请求的服务器地址;
 */
let kyle_config = {
  baseApiUrl,
  basePath,
  proApiUrl: baseApiUrl + basePath,
  devApiUrl: basePath
};
